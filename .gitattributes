# Check text files out with LF on every platform. Without this a Windows clone with
# core.autocrlf=true gets a CRLF working tree, and `npm run lint` then reports a
# formatting error on every file while CI on Linux stays green.
* text=auto eol=lf
